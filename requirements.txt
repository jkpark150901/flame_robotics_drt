pyqt6
pyqtdarktheme
numpy
scipy
scikit-image
pyzmq
colorlog
open3d
vedo
pyyaml
opencv-python
trimesh
networkx
lxml
pycollada
matplotlib

# OMPL joint-space path planners. Version is pinned to the migration baseline.
# Official OMPL 2.0.0 wheels support Linux/macOS (CPython 3.10+), but not Windows.
# On Windows, build the official OMPL tag 2.0.0 Python binding and install it
# into this environment manually before selecting an OMPL-backed planner.
ompl==2.0.0; python_version >= "3.10" and platform_system != "Windows"

# Robotics Pinocchio. PyPI package `pin` currently provides pip wheels on Linux only.
pin; platform_system == "Linux"
